#include <stdlib.h>
#include <stdio.h>
#include <string.h>

void bsort(char** t, int taille)
{
    int len_max=0;
    for (int i=0; i<taille; i+=1) 
    {
        if (strlen(t[i])>len_max) len_max=strlen(t[i]);
    }

    void base(int i, char** t1, int taille1)
    {
        
    }
}

    
